{{#each item}}
    <div class="cart-item__cart" id="cart-item_cart/{{this.id}}">
        <div class="cart-item__cart__checkbox">
            <label class="item-cart__selection">
                <input type="checkbox" class="checkbox-opt" id="item_cart__select/{{this.id}}" name="itemCart" checked>
            </label>
        </div>

        <a href="/product/{{this.id}}" class="cart-item__cart__img">
            <img src="{{this.imgsrc}}" alt="{{this.name}} photo" class="cart-item__cart__img-src">
        </a>

        <div class="cart-item__cart__main">
            <a href="/product/{{this.id}}" id="{{this.id}}" class="link__no-color cart-item__cart__id">
                <span>{{this.name}}</span>
            </a>
            <a href="" class="link__no-color">
                <span class="vendor-cart__text">{{#if this.vendor}}{{this.vendor}}{{else}}Reazon{{/if}}</span>
            </a>
            <div class="cart-item__cart__bottom">
                <label class="item-cart__selection">
                    <input type="checkbox" class="favourite-opt" {{#if this.favourite}} checked {{/if}}>
                    <span id="favourite-opt_cart__text">{{#if this.favourite}}
                        В избранном {{else}} В избранное {{/if}}</span>
                </label>
                <button class="button-default-no-box delete-cart-item__down item-cart__selection link__no-color"
                        id="button-default_cart__delete">
                    <img src="/img/Bin.svg" alt="delete item from cart button" class="button-delete-cart__img">
                    <span class="delete-cart-item" id="delete-cart-item/{{this.id}}">Удалить</span>
                </button>
            </div>
        </div>

        <div class="count-selector">
            <button class="button-notext">
                <span class="link__no-color" id="button-minus_cart/{{this.id}}">−</span>
            </button>
            <div class="count-selector__amount">
            <span class="text-normal-default__bold count-product" id="count-product/{{this.id}}">
                {{this.count}}
            </span>
            </div>
            <button class="button-notext">
                <span class="link__no-color" id="button-plus_cart/{{this.id}}">+</span>
            </button>
        </div>

        <div class="cart-item__cart__price">
            <span class="text-normal-very-large" id="price/{{this.id}}">{{#if this.strLowprice}}{{this.strLowprice}}
                ₽{{else}}{{this.strPrice}}
                ₽{{/if}}</span>
            <span class="text-normal-large__strikethrough item_cart__salePrice" id="sale-price/{{this.id}}">{{#if
                    this.strLowprice}}{{this.strPrice}}
                ₽{{/if}}</span>
        </div>
    </div>
{{/each}}
